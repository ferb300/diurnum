# Abi-Zitatesammler
Stellt eine einfache Weboberfläche bereit, bei der sich Zitate in eine Datenbank eintragen lassen.

## Setup
nach erstem start trusted domains in config.php der nextcloud eintragen (domain und container name) und neustarten